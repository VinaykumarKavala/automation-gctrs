# API
api.base=
api.timeout.ms=20000

# UI
ui.base=
ui.username=
ui.password=
ui.explicitWait.s=10
ui.implicitWait.s=0

# Selenium
sel.browsers=chrome
sel.headless=false
sel.pageLoadStrategy=NORMAL
sel.remote.enabled=false
sel.remote.url=

# Parallel
par.mode=none
par.threads=4
par.dpThreads=4
par.forks=1
par.forkEvery=0

# Retries & logging
retry.count=0
report.logs.level=INFO
